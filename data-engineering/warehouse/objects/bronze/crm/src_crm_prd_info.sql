CREATE TABLE IF NOT EXISTS `src_crm_prd_info`
(
    `id`         INT,
    `key`        VARCHAR(50),
    `nm`         VARCHAR(50),
    `cost`       VARCHAR(50),
    `line`       VARCHAR(50),
    `start_date` VARCHAR(50),
    `end_date`   VARCHAR(50)
);
